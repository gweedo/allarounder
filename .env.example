# Copy this file to .env and fill in the values.
# Never commit .env to version control.

# The frontend is a static export (src/frontend) and needs no environment
# variables to build or run locally — it reads content/ from disk.
#
# The content pipeline (src/pipeline, in progress — see NEXT-STEPS-PROMPT.md
# Task 3) will need Google service-account credentials, a Drive folder ID,
# and a Sheet ID once built. Those secrets are never committed; they live in
# GitHub Actions secrets and Apps Script script properties.
